# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Parsetragram' do
    # Add Parse pods and install
    platform :ios, "15.2"
    use_frameworks!
    pod 'Parse'
    pod 'AlamofireImage'
end


